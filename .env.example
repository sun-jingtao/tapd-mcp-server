# npm 发布脚本使用。仅维护者需要填写，不要提交真实 token。
NODE_AUTH_TOKEN=your_npm_token

# MCP 客户端运行 server 时注入。通常配置在 .cursor/mcp.json 或 .vscode/mcp.json 的 env 中。
TAPD_ACCESS_TOKEN=your_tapd_access_token

# 可选。默认已启用 Sentry；设为空字符串可关闭，或填其它 DSN 覆盖默认目标。
# TAPD_MCP_SENTRY_DSN=
